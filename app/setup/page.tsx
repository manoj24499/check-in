import { SetupForm } from '@/app/components/setup/setup-form';

export default function SetupPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background p-6">
      <SetupForm />
    </main>
  );
}